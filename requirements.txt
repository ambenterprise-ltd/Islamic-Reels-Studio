# ==============================================================================
# Islamic Reels Studio - Comprehensive Dependencies & Requirements
# ==============================================================================

# --- GUI, System Tray & Visual Assets ---
customtkinter>=5.2.0           # Modern UI framework for studio_gui.py
pystray>=0.19.5                # Background system tray minimization & control
Pillow>=10.0.0                 # Image processing & thumbnail generation (provides PIL)

# --- Video Editing & Media Pipelines ---
# CRITICAL: moviepy must be < 2.0.0 (MoviePy v2 dropped moviepy.editor and broke legacy clip APIs)
moviepy>=1.0.3,<2.0.0          # Core video composition, audio binding, and effects
opencv-python>=4.8.0           # Computer vision & frame processing (provides cv2)
html2image>=2.0.4              # HTML/CSS-based animated subtitle rendering via headless browser
yt-dlp>=2024.8.0               # High-speed video extraction for TikTok Repurposer
curl-cffi>=0.7.0               # Chrome TLS fingerprint impersonation for yt-dlp bot bypass

# --- Audio Engine & AI Synchronization ---
edge-tts>=6.1.0                # Neural speech synthesis for Quranic translations
faster-whisper>=1.0.0          # CTranslate2-accelerated Whisper for word-level subtitle alignment
pedalboard>=0.8.0              # Spotify audio DSP engine for acoustic reverb & vocal enhancement

# --- Network, Cloud APIs & Social Media Uploaders ---
requests>=2.31.0               # HTTP client for Quran Cloud API, webhooks & CDN uploads
google-api-python-client>=2.100.0  # Official YouTube Data API v3 client
google-auth-oauthlib>=1.0.0    # OAuth 2.0 desktop authentication flow for YouTube
google-auth-httplib2>=0.2.0    # Transport adapter for Google API authentication
gspread>=6.0.0                 # Google Sheets multi-tab cloud reporting & ledger syncing
oauth2client>=4.1.3            # Legacy/fallback service account credential handler
discord.py>=2.3.0              # Discord bot listener for remote pipeline triggers

# --- Hardware Optimization & System Utilities ---
psutil>=5.9.0                  # System RAM/CPU monitoring for dynamic thread throttling
proglog>=0.1.10                # Progress logger for MoviePy render status callbacks
numpy>=1.24.0,<2.0.0           # Array math for frames (must be <2.0.0 for MoviePy/OpenCV compatibility)

# --- Optional Hardware Acceleration ---
# torch>=2.0.0                 # Optional: Enables NVIDIA CUDA GPU detection in hardware_optimizer.py
